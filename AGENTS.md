# YouTube Data Tools

## Agent skills

### Issue tracker

Specs and tickets use GitHub Issues. Assignment indicates active ownership. See `docs/agents/issue-tracker.md`.

### Triage labels

Use `bug`, `enhancement`, `spec`, and the configured readiness and disposition labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository using root `CONTEXT.md` and `docs/adr/` when created. See `docs/agents/domain.md`.
